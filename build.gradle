plugins {
    id("maven-publish")
    id("org.jetbrains.kotlin.jvm").version("latest.release")
}

group = "net.auoeke"
version = "0.2.0"

sourceCompatibility = 16
targetCompatibility = 16

sourceSets {
    main {
        kotlin.srcDir("src")
    }

    test {
        kotlin.srcDir("generator")
    }
}

repositories {
    mavenCentral()

    maven {url = "https://maven.auoeke.net"}
}

dependencies {
    testImplementation("net.auoeke:extensions:latest.integration")
    testImplementation("org.junit.jupiter:junit-jupiter-api:latest.release")
    testRuntimeOnly("org.junit.jupiter:junit-jupiter-engine")
}

tasks.withType(compileKotlin.class) {
    kotlinOptions.jvmTarget = targetCompatibility
}

publishing {
    repositories {
        maven {
            url = "https://maven.auoeke.net"

            credentials {
                username = System.properties["maven.username"]
                password = System.properties["maven.password"]
            }
        }
    }

    publications {
        main(MavenPublication) {
            from(components.java)
        }
    }
}
